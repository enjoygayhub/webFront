# BackEnd

## 语言

### Node 单线程怎么处理并发请求
Node.js 是单线程 + 事件循环 + 非阻塞异步 IO处理并发。

1：使用异步 API，避免同步计算
例如fs/promises、数据库异步驱动
2：使用 worker_threads 工作线程
把 CPU 密集任务丢给独立子线程，主线程继续处理请求。
3：集群模式 cluster / PM2 多进程（最常用）
利用多核 CPU，开启多个 Node 进程，每个进程一个单线程。PM2 一键启动多进程：
4：把 CPU 任务抽成独立服务
比如图片处理、大数据计算，单独开 Go/Java 服务，Node 只做接口转发。

### node java go对比
维度	Node.js	Java	Go
并发模型	单线程事件循环	多线程	协程Goroutine
性能	IO强，CPU弱	均衡	CPU/IO都极强
开发速度	最快	慢	快
内存占用	中	高	最低
部署	需Node环境	需JDK	单文件直接跑
适合场景	全栈、实时、IO 接口	企业级、复杂业务、金融	微服务、云原生、高性能网关
缺点	CPU 差、大型项目难维护	笨重、启动慢、啰嗦	生态弱、业务组件少

## MySQl

### 范式（规范化）
第一范式（1NF）：列不可再分（原子性）
第二范式（2NF）：消除部分依赖，非主键列必须依赖整个主键
第三范式（3NF）：消除传递依赖，非主键列之间不能互相依赖

一般，规范化数据通常写入更快（只有一个副本），但查询更慢（它需要连接）；反规范化数据通常读取更快（连接更少），但写入更昂贵（更多副本要更新，使用更多磁盘空间）。
在中小规模的系统中，规范化数据模型通常是最好的，因为你不必担心保持数据的多个副本相互一致，执行连接的成本是可以接受的。然而，在非常大规模的系统中，连接的成本可能会成为问题。

多对多关系通常需要"双向"查询
星型模式：当表关系被可视化时，事实表位于中间，被其维度表包围；到这些表的连接就像星星的光芒。
雪花模式：星型模式中的维度被进一步分解为子维度

###  索引
MySQL InnoDB 索引用 B + 树
B 树：每个节点存 key + 数据
B + 树：只有叶子节点存数据，非叶子只存索引键
优点：
层级更少，IO 次数更少
叶子节点链表相连，范围查询极强

索引分类
主键索引（聚簇索引）：叶子存整行数据，一张表只有 1 个
普通 / 唯一索引（二级索引）：叶子存主键值
最左前缀原则联合索引 (a,b,c)，只能用到：
where a=?
where a=? and b=?
where a=? and b=? and c=?
跳过 a 直接查 b,c 索引失效

索引失效场景（高频）
like %xxx 前缀模糊
字段类型隐式转换（字符串不加引号）
or 两边字段都没索引
not in、!=、is not null
对索引列做函数运算：year(create_time)
联合索引不满足最左前缀

### 事务 ACID & 隔离级别

事务中的所有读写操作被视作单个操作来执行：整个事务要么成功（提交），要么失败（中止、回滚）。
A 原子性：要么全成功，要么全失败
C 一致性：事务前后数据约束不变，取决于应用程序如何使用数据库
I 隔离性：多个事务互不干扰，可串行划，
D 持久性：提交后永久落地

隔离级别	脏读	读偏差	幻读	丢失更新	写偏差
读未提交	✗ 可能	✗ 可能	✗ 可能	✗ 可能	✗ 可能
读已提交	✓ 防止	✗ 可能	✗ 可能	✗ 可能	✗ 可能
快照隔离	✓ 防止	✓ 防止	✓ 防止	? 视情况	✗ 可能
可串行化	✓ 防止	✓ 防止	✓ 防止	✓ 防止	✓ 防止



7. 事务四大问题：脏读、不可重复读、幻读、死锁


不可重复读：：生成mvcc快照，同一事务使用相同快照
幻读：一个事务中的写入改变另一个事务中搜索查询的结果：谓词锁，
死锁：两个事务互相持有对方需要的锁解决：超时机制、死锁检测、统一锁顺序

脏读：读到另一个事务没提交的数据：锁

脏写：一个事务覆盖另一个已写入但尚未提交的数据。几乎所有事务实现都防止脏写。

读取偏差/不可重复读：
同一事务，两次查询结果不一样。通过快照隔离来防止，它允许事务从对应于特定时间点的一致快照读取。它通常使用多版本并发控制（MVCC）实现。

幻读
事务读取匹配某些搜索条件的对象。另一个进行影响该搜索结果的写入。快照隔离防止直接的幻读，但写偏差上下文中的幻读需要特殊处理，例如索引范围锁。

丢失更新：
两个并发执行读-修改-写循环。一个覆盖另一个的写入而不合并其更改，因此数据丢失。某些快照隔离的实现会自动防止此异常，而其他实现需要手动锁（SELECT FOR UPDATE）。

写偏差：
事务读取某些内容，根据它看到的值做出决定，并将决定写入数据库。但是，在进行写入时，决策的前提不再为真。只有可串行化隔离才能防止此异常。



3. MVCC 多版本并发控制（加分项）
InnoDB 靠 MVCC 实现可重复读，不加锁也能并发：
每行记录隐藏字段：DB_TRX_ID（事务 ID）、DB_ROLL_PTR（回滚指针）
每次更新生成 undo log 历史版本
事务通过版本链读取历史快照，实现不加锁读

4. MySQL 锁机制
行锁：InnoDB，粒度小，并发高，只锁行
表锁：MyISAM，粒度大，并发差
意向锁：InnoDB 自动加，防止表锁和行锁冲突
悲观锁：select ... for update
乐观锁：版本号 version，不加锁，提交判断版本

5. 慢查询 & SQL 优化（工作必用）
慢查询：执行时间超过 long_query_time 的 SQL
开启慢查询日志定位问题 SQL

优化手段：
避免 select *，只查需要字段
分页优化：limit 100000,10 改成主键偏移
join 字段必须加索引
避免大事务、长连接
批量插入代替循环单条插入
大表分库分表（简单提一句即可）


## Redis 
1. 5 种常用数据结构 & 场景
String：缓存、计数器、分布式 ID、Token
Hash：用户信息、对象存储（user:{name,age}）
List：消息队列、排行榜、栈
Set：去重、好友共同关注
Sorted Set(zset)：排行榜、延迟队列

2. Redis 过期删除策略
惰性删除：访问时才判断过期删除（省 CPU，占内存）
定期删除：随机抽查过期 key 删除（均衡 CPU 和内存）
Redis 采用 惰性 + 定期 组合策略

3. 持久化机制（RDB / AOF）
RDB：定时快照，二进制文件，恢复快，可能丢最后一段时间数据
AOF：记录每条写命令，文本日志，几乎不丢数据，文件大
生产环境一般 RDB+AOF 混合持久化

4. 缓存穿透、击穿、雪崩
① 缓存穿透
现象：查询数据库不存在的数据，绕过缓存直接打 DB例：查 id=-1、id=9999999解决方案
缓存空值（过期短一点）
布隆过滤器（把所有合法 id 提前存进去）
接口参数校验
② 缓存击穿
现象：热点 Key 刚好过期，大量并发请求同时打到 DB例：秒杀商品缓存 1 小时到期，瞬间几万请求查 DB解决方案
互斥锁（Redis setnx）
热点 Key 永不过期
提前主动刷新缓存
③ 缓存雪崩
现象 1：大量 Key 同时过期 → 全部查 DB现象 2：Redis 宕机解决方案
过期时间加随机值，避免同一时间过期
Redis 集群保证高可用
多级缓存（本地缓存 + Redis）
接口限流、熔断降级

5. Redis 为什么快？
纯内存操作
单线程模型，无锁竞争开销
IO 多路复用（epoll）
简单高效数据结构


7. Redis 单线程为什么能支持高并发？
Redis 核心命令处理是单线程，但网络 IO 是多路复用
持久化、集群、异步任务是多线程
单线程避免上下文切换，纯内存操作所以快。

8. Redis 过期淘汰策略（内存满了怎么办）
volatile-lru：过期 key 中淘汰最少使用
allkeys-lru：所有 key 淘汰最少使用（最常用）
volatile-ttl：淘汰快过期的
random、noeviction（报错）

## 系统设计
性能
关键指标：响应时间（用户感知）、吞吐量（处理能力）
必须看百分位点（p50/p95/p99），而非平均值，关注尾部延迟
避免重试风暴、亚稳态故障，用熔断、限流、指数退避保护系统。

可靠性：故障可容忍，系统不失效
故障类型：硬件故障（可冗余）、软件故障（关联强、难预防）、人为错误（配置变更为主因）

可伸缩性：先量化负载（QPS、读写比等）
扩展方式：纵向伸缩（更强机器）、横向伸缩（无共享分布式）
